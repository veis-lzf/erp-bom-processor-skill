***

name: "erp-bom-processor"
description: "处理PCB BOM文件，进行元件库匹配、值/封装搜索和格式生成。当用户需要将原理图BOM转换为格式化的ERP BOM时调用此工具。"
--------------------------------------------------------------------------------

# ERP BOM处理器

## 概述

处理从原理图工具导出的PCB BOM，与元件库（系统电子料库）进行匹配，并生成格式化的ERP BOM文件。

**核心功能**：
- 有料号的通过料号从电子料库索引最新物料描述
- 无料号的通过物料属性值(Value)+器件封装(Footprint)匹配，提取料号和描述
- 电阻/电容/磁珠/电感/晶振等元件类型自动识别与匹配
- 电阻优先匹配1%精度，电容无满足耐压时匹配更高耐压
- 电感按感值+额定电流+封装匹配
- 相同料号自动合并，位号用逗号拼接
- 中英文BOM列名格式兼容
- NP/NC/NM 空贴物料检测与标记（输出备注列）
- 多BOM差异比对，生成差异清单List

## 目录结构

```
erp-bom-workspace/
├── 01_Template/          # BOM模板参考文件
├── 02_BOMfromSystem/     # 元件库文件（使用最新日期的文件）
├── 03_order/             # 待处理的BOM文件
├── 04_output/            # 处理完成后的输出文件路径
├── bom_processor.py      # 核心处理脚本
└── bom_diff.py           # BOM比对脚本
```

## BOM列映射

支持两种列名格式：

### 英文格式

| 输入列           | 内部列名       | 描述       |
| ------------- | ----------- | -------- |
| Item          | Item        | 序号       |
| Part NO.      | Part NO.    | 元件料号（可选） |
| Part Name     | Part Name   | 元件描述（输入） |
| Quantity      | Quantity    | 元件使用数量   |
| Reference     | Reference   | 元件位号     |
| PCB Footprint | PCB Footprint | 器件封装     |
| Value         | Value       | 元件属性值    |

### 中文格式

| 输入列 | 内部列名 | 描述       |
| --- | --- | -------- |
| 序号  | Item | 序号       |
| 编码  | Part NO. | 元件料号（可选） |
| 名称  | Part Name | 元件描述（输入） |
| 数量  | Quantity | 元件使用数量   |
| 位号  | Reference | 元件位号     |
| 封装  | PCB Footprint | 器件封装     |
| 规格  | Value | 元件属性值    |

## 处理规则

### 总体流程图

```
BOM输入
  │
  ├─ 跳过条件：位号TP开头 或 值包含NC
  │
  ├─ 有料号 ──→ 通过料号搜索库 ──→ 验证电阻值 ──→ 找到则用
  │                                                    │
  └─ 无料号或料号无效 ──→ 值+封装匹配 ──→ 类型分类 ──→ 类型特定匹配
                                                              │
                                                    ┌─── 找到 → 提取料号+描述
                                                    └─── 未找到 → 标记"未找到匹配"
```

### 1. 料号匹配

- 如果提供了料号，首先通过料号搜索
- 如果找到，验证电阻值是否匹配（针对电阻）
- 如果未找到或无效，使用值+封装匹配

### 2. 值+封装匹配

#### 元件类型分类（严格优先级顺序）

按以下**优先级顺序**判断元件类型，一旦匹配即进入对应类型的匹配函数：

1. **IC/芯片** — 值以以下开头：RTL、STM、ATMEGA、ESP、GD32、NXP、TI、MICROCHIP、CA-
2. **晶振** — 封装满足以下任一条件：
   - 以 `X4-` 开头（如 `X4-3225`）
   - 包含 `_X4_`
   - 以 `_X4` 结尾
   - 包含 `XTAL` 或 `OSC`
3. **磁珠(FB)** — 位号以 `FB` 开头，或封装中包含 `FB`
4. **电感** — 封装以 `IND` 开头，或值中包含 `UH`/`NH`
5. **电阻** — 封装以 `R` 开头，或包含 `RES`，或值中包含 `OHM`，或同时包含 `K` 和 `F`，或包含 `R/`，或4位数字后跟 `R`，或包含 `M/F`，或封装结尾是 `R` 且前面是数字
6. **电容** — 封装以 `C` 开头，或包含 `CAP`，或值中包含 `F`（排除以下情况：K+F、R/、M/、RTL/STM开头、FB位号、含`_`/`-`且非电压结尾）
7. **通用元件** — 其他所有元件

> **注意**：`MHZ` 值作为磁珠的补充判断（在电容判断之后），仅当以上条件都不满足且值包含 `MHZ` 时才判断为磁珠。例如 `330R/100MHZ` 且位号 `FB` 开头会优先走磁珠判断。

#### 电阻匹配规则

**值解析流程** (`parse_resistor_value`)：
1. 精度识别：`1%`/`F` → 1%，`5%`/`J` → 5%，默认5%
2. 清理值：去除 `1%`/`5%`/`F`/`J`/`/0.5W`/`/100MHZ` 等后缀
3. 单位匹配（优先级从高到低）：
   - `MR` → 兆欧（×1000000）
   - `mR` → 毫欧（÷1000），**优先于 `m`** 避免误判
   - `m` → 毫（÷1000）
   - `M` → 兆（×1000000）
   - `K` → 千（×1000）
   - `R/`、`R`+结束/空格/下划线 → 欧姆
   - `OHM` → 欧姆
   - 纯数字带Ω → 欧姆

**分隔符支持**：`_`、`-`、`/` 作为分隔符，例如 `60.4R_5%`、`10mR/F/0.5W`、`3M/F`

**库中值提取** (`extract_resistance_value`)：
- 从规格型号中提取电阻值，如 `100mΩ,±1%,1/2W,1210,厚声,...` → 100mΩ → 0.1Ω
- 支持 KΩ、MΩ、Ω、mΩ 单位

**功率解析** (`parse_power`)：
- 从 `/(\d+\.?\d*)W` 中提取功率值
- 转换：0.0625W→1/16W，0.125W→1/8W，0.25W→1/4W，0.5W→1/2W，1.0W→1W

**封装格式兼容**：
- `R0805` → 提取 `0805`
- `0805R` → 提取 `0805`
- 在库规格中匹配 `0805` 即为匹配

**严格匹配规则**：
- 如果BOM指定了封装（如 `R0805`），则库物料规格中必须包含 `0805`
- 库中无对应封装物料时 → **返回None**（不会匹配其他错误封装的物料）
- 优先级：正确封装(必须) > 精度优先(1%)

#### 电容匹配规则

**值解析流程** (`parse_capacitor_value`)：
- 正则：`^([\d.]+)\s*([PFNU]F?)\s*(?:[/_-]?(\d+V))?$`
- 支持分隔符：`/`、`_`、`-`
- 示例：`100nF/50V`、`100pF_50V`、`10uF-50V`、`330pF`、`47uF 16V`
- 单位转换：PF/P→×1，NF/N→×1000，UF/U/μF→×1000000
- 电压提取：`50V` → 50

**库中值提取** (`extract_capacitance_value`)：
- 从规格中提取容值，如 `47uF,±10%,16V,X5R,1210,...` → 47uF
- 单位转换与解析一致

**电压提取** (`extract_voltage_from_spec`)：
- 从规格中提取电压值，如 `16V` → 16
- 正则：`(?:^|，|,)(\d+)V(?:,|$|\s)`

**封装格式兼容**：
- `C0805` → 提取 `0805`
- `0805C` → 提取 `0805`
- `1210C` → 提取 `1210`（通过 `C?(\d{4})C?` 正则）

**严格匹配规则**：
- 如果BOM指定了封装（如 `C0402`），则库物料规格中必须包含对应封装编号
- 库中无对应封装物料时 → **返回None**
- 优先级：正确封装(必须) > 最近电压(0)，电压为None时视为 `float('inf')`

#### 晶振匹配规则

**频率匹配**：
- 从值中提取频率数字：`40Mhz` → `40`
- 从库规格中提取频率数字：`40MHz` → `40`
- **必须完全匹配**（字符串相等），避免24MHz匹配到40MHz

**封装匹配**：
- 从封装中提取4位数字编号：`X4-3225` → `3225`
- 库规格中包含该编号即为封装匹配

**多结果支持**：
- `find_crystal` 支持 `return_all=True` 参数，返回所有匹配候选
- 优先级：正确封装(2) > 仅频率匹配(0)

#### 磁珠匹配规则

**识别条件**：
1. 位号以 `FB` 开头（如 `FB1`、`FB2`）
2. 封装中包含 `FB`
3. 值中包含 `MHZ` 作为补充判断（如 `330R/100MHZ`）

**严格匹配规则**：
- 从封装中提取4位数字编号
- 库物料名称需包含 `磁珠`/`BEAD`/`FERRITE`
- 库物料规格中必须包含对应封装编号
- 无匹配时 → **返回None**

#### 电感匹配规则

**识别条件**：
1. 封装以 `IND` 开头（如 `IND_201610`、`IND_252012`）
2. 值中包含 `UH`（微亨）或 `NH`（纳亨）

**值解析流程** (`find_inductor`)：
- 支持格式：`4.7uH`、`2.2uH`、`47nH`、`1uH`、`4.7uH/4A` 等
- 单位统一转换为 nH 进行比较：
  - `UH`/`uH` → ×1000
  - `NH`/`nH` → ×1
- 解析额定电流：`/4A`、`,4A` → I=4A

**匹配规则**：
- 库物料名称需包含 `电感`/`绕组`/`INDUCTOR`/`IND`
- 电感值必须精确匹配（≤0.1% 误差）
- 封装匹配加分（+5），额定电流匹配加分（+3）
- 按总分排序取最高分，无匹配时 → 回退到 `find_generic`

#### 通用元件匹配 (`find_generic`)

**特殊字符处理**：
- 去除 `_` 和 `-` 后进行模糊匹配
- 解析 `HFD4_5-SR` → `HFD45SR` 进行匹配

**封装关键词提取**：
- `BAT_XT30PWPW2_PB` → 提取 `XT30PW`（优先匹配卧式）
- `BAT_XT30PBPB2_PB` → 提取 `XT30PB`
- 含 `XT30` 或 `XT60` 的封装也会匹配

**评分机制**（按总分排序，取最高分）：
| 条件 | 分值 |
| --- | --- |
| 值在规格中（含模糊匹配） | 2 |
| 值在名称中 | 1 |
| 封装在规格或名称中 | 1 |
| 封装关键词在规格中 | 3 |

#### IC匹配规则 (`find_ic`)

- 值或规格互相包含即视为候选
- 优先返回规格中精确包含值的物料
- 支持型号如 `RTL8762CMF`、`STM32F103`、`CA-IF1051VS` 等

### 3. 特殊跳过情况

- **位号以"TP"开头**（测试点，不参与BOM处理）
- **值整体等于 `NC`/`NP`/`NM`**（空贴/不焊接，跳过）
- **值包含 `/NC`、`/NP`、`_NC`、`_NP`、`NC_`、`NP_`**（疑似空贴，保留行并在备注列标记）

### 4. 备注列

输出BOM中包含 `备注` 列，用于标记：
- `⚠️请确认是否空贴` — 值包含 `/NC`、`/NP`、`_NC`、`_NP` 等疑似空贴标记，需要人工确认

### 5. 相同料号合并

- 合并相同料号的行（无料号的不合并）
- 数量求和
- 位号用逗号连接（如 `R1,R4,R7`）

## 核心函数说明

### 值解析函数

| 函数 | 输入 | 输出 | 说明 |
| --- | --- | --- | --- |
| `parse_resistor_value(value_str)` | 电阻值字符串 | `(value_ohms, precision)` | 解析电阻值和精度，支持多种格式 |
| `parse_capacitor_value(value_str)` | 电容值字符串 | `(value_pf, voltage)` | 解析电容值和电压，支持多种分隔符 |
| `parse_power(value_str)` | 值字符串 | 功率分数或None | 解析功率值，如 `/0.5W` → `1/2W` |

### 库值提取函数

| 函数 | 说明 |
| --- | --- |
| `extract_resistance_value(spec)` | 从库规格中提取电阻值（Ω） |
| `extract_capacitance_value(spec)` | 从库规格中提取电容值（pF） |
| `extract_voltage_from_spec(spec)` | 从库规格中提取电压值（V） |
| `validate_resistor_match(component, value_str)` | 验证料号匹配的电阻值是否一致 |

### 匹配函数

| 函数 | 说明 |
| --- | --- |
| `find_component_by_part_no(lib, part_no)` | 通过料号精确查找 |
| `find_component_by_value_footprint(lib, value, footprint, reference)` | 值+封装匹配，自动类型分类 |
| `find_ic(lib, value_str, footprint_str)` | IC/芯片匹配 |
| `find_resistor(lib, value_str, footprint_str)` | 电阻匹配（含封装严格匹配） |
| `find_capacitor(lib, value_str, footprint_str)` | 电容匹配（含封装严格匹配） |
| `find_bead(lib, value_str, footprint_str)` | 磁珠匹配（含封装严格匹配） |
| `find_inductor(lib, value_str, footprint_str)` | 电感匹配（感值+电流+封装，无匹配回退通用） |
| `find_crystal(lib, value_str, footprint_str, return_all=False)` | 晶振匹配（频率精确匹配） |
| `find_generic(lib, value_str, footprint_str)` | 通用元件匹配（模糊匹配+评分） |

### 处理函数

| 函数 | 说明 |
| --- | --- |
| `merge_by_part_no(df)` | 合并相同料号的数据行 |
| `process_bom(bom_file, lib_file, output_dir, merge_same_part_no=True)` | 主处理流程 |

### 比对函数 (`bom_diff.py`)

| 函数 | 说明 |
| --- | --- |
| `load_bom_files(file_paths)` | 加载多个processed BOM文件 |
| `build_diff_data(bom_data)` | 构建以Part NO.为索引的比对数据结构 |
| `generate_diff_report(...)` | 生成差异报告（汇总/独有/共有差异/未匹配） |
| `write_diff_excel(...)` | 输出格式化的差异清单Excel |
| `bom_diff(file_paths, output_path)` | 比对主入口函数 |

## 调试辅助脚本

工作区中包含以下调试脚本，用于验证特定功能：

| 脚本 | 用途 |
| --- | --- |
| `check_all_fp.py` | 检查所有元件的封装匹配情况 |
| `verify_fixes.py` / `verify_latest_fixes.py` | 验证修复是否生效 |
| `debug_bead.py` / `debug_bead2.py` | 磁珠匹配调试 |
| `debug_cap_format.py` / `debug_cap_match.py` | 电容格式/匹配调试 |
| `debug_resistor.py` / `debug_resistor_match.py` | 电阻匹配调试 |
| `debug_hfd4.py` / `test_hfd4.py` | HFD4继电器匹配调试 |
| `debug_process.py` | 完整处理流程调试 |
| `verify_output.py` | 输出结果验证 |

## 使用方法

1. 确保工作区结构正确
2. 将BOM文件放入 `03_order/` 目录
3. 将最新元件库放入 `02_BOMfromSystem/` 目录
4. 运行 `bom_processor.py`
5. 输出文件将在 `04_output/` 目录，文件名带有 `_processed.xlsx` 后缀

### 自定义配置

编辑 `bom_processor.py` 底部的 `__main__` 部分：

```python
BOM_FILES = [
    '03_order/你的BOM文件.xlsx',
]
LIB_FILE = '02_BOMfromSystem/系统电子料库_最新日期.xlsx'
OUTPUT_DIR = '04_output'
```

### 批量处理

`bom_processor.py` 默认支持批量处理多个BOM文件，只需在 `BOM_FILES` 列表中添加文件路径即可。

## BOM比对功能

通过分析多份已处理的BOM文件，自动生成差异清单报告，快速识别BOM之间的差异。

### 比对逻辑

1. 以 `Part NO.` 为主键进行比对
2. 无料号的物料归入"未匹配物料"类别
3. 分类输出四种结果

### 输出报告结构

| Sheet名称 | 说明 |
| --- | --- |
| `汇总统计` | 各BOM的总物料数、已匹配/未匹配数量、匹配率 |
| `独有物料` | 仅在某一BOM中出现（其他BOM中不存在的料号），按BOM名称彩色标记 |
| `共有物料差异` | 多个BOM共有的物料，但数量或位号存在差异，黄色高亮标记 |
| `未匹配物料` | 所有BOM中未携带有效料号的物料，红色高亮标记 |

### 使用方法

```bash
# 自动读取04_output下所有 _processed.xlsx 文件进行比对
python bom_diff.py

# 手动指定文件路径
python bom_diff.py 04_output/BOM_A_processed.xlsx 04_output/BOM_B_processed.xlsx
```

### 输出

- 在 `04_output/` 目录生成 `BOM差异清单.xlsx`，包含4个Sheet页
- 终端同步打印汇总统计和差异数量

## 版本存档

| 版本 | 文件 | 说明 |
| --- | --- | --- |
| **v1.0 (当前稳定版)** | `bom_processor.py` (工作脚本) / `bom_processor_v1.0.py` (存档副本) | 完整功能实现，包含所有匹配规则和修复，3份BOM自测通过 |
| **v1.1** | `bom_diff.py` (新增) | 新增BOM比对功能，支持多BOM差异分析，输出差异清单Excel |
| **v2.1.0** | `bom_processor.py` (更新) | 新增电感匹配(`find_inductor`)、NP/NC/NM空贴检测与备注列、优化NC值处理、CLI参数支持 |

## 关键修复记录

### 分类判断修复

1. **HFD4_5-SR继电器误判为电容** ← 修复分类优先级
   - 问题：值含 `F` 被误判为电容，封装 `DFN8_HFD4-5-SR` 以 `R` 结尾被误判为电阻
   - 修复：电容分类条件排除含 `_`/`-` 且非电压结尾的值；电阻分类检查封装结尾 `R` 前是否为数字

2. **CA-IF1051VS IC误判为晶振** ← 修复X4判断精度
   - 问题：封装 `SOIC8_5X4X1P8_1P27` 包含 `X4` 被误判为晶振
   - 修复：晶振封装判断改为精确模式（`X4-`开头、`_X4_`中间、`_X4`结尾）

3. **磁珠分类冲突** ← 优化分类顺序
   - 问题：磁珠和晶振的判断条件都包含 `MHZ`，导致330R/100MHZ磁珠被误判
   - 修复：调整分类顺序为 IC→晶振(封装判断)→磁珠(位号/封装)→电阻→电容→MHZ补充→通用

### 值解析修复

4. **10mR/F/0.5W毫欧电阻解析为10MΩ** ← 修复正则优先级
   - 问题：`mR` 被 `M` 正则捕获，匹配为兆欧
   - 修复：`MR`→兆欧优先匹配，`mR`→毫欧其次（区分大小写），`m`→毫，`M`→兆

5. **60.4R_5%电阻下划线格式解析失败** ← 修复分隔符支持
   - 问题：下划线分隔格式未被 `R` 正则捕获
   - 修复：`R` 正则增加 `_` 作为结束符：`R(?:/|$|\s|_)`

6. **100pF_50V电容下划线格式解析失败** ← 修复分隔符支持
   - 修复：`parse_capacitor_value` 正则增加 `_` 和 `-` 分隔符支持

7. **电容斜杠分隔符（100nF/50V）解析失败** ← 修复分隔符支持
   - 修复：正则增加 `/` 分隔符支持

8. **电容None值排序错误** ← 修复排序兼容性
   - 问题：电压为None时 `sort` 抛出 `TypeError`
   - 修复：None值视为 `float('inf')`

### 封装匹配修复

9. **1210C电容封装未匹配** ← 修复封装格式兼容
   - 问题：`1210C` 未识别为1210封装
   - 修复：正则改为 `C?(\d{4})C?`，兼容 `C1210`、`1210C`、`0805` 等格式

10. **电阻封装严格匹配** ← 新增严格匹配机制
    - 当BOM指定封装时，库中必须找到对应封装的物料
    - 无匹配时返回None，不会匹配其他封装的物料

11. **电容封装严格匹配** ← 新增严格匹配机制
    - 与电阻相同的严格匹配逻辑

12. **磁珠封装严格匹配** ← 新增严格匹配机制
    - 与电阻相同的严格匹配逻辑

### 数据合并修复

13. **相同料号未合并** ← 新增合并功能
    - 新增 `merge_by_part_no` 函数
    - 合并后数量求和，位号用逗号拼接

### 通用匹配修复

14. **XT30_2-2连接器未匹配** ← 新增封装关键词提取
    - 从封装 `BAT_XT30PWPW2_PB` 提取 `XT30PW` 关键词
    - 优先匹配卧式连接器

15. **IC型号CA-识别** ← 新增IC前缀
    - 新增 `CA-` 开头的IC型号识别

16. **中文列名支持** ← 新增列名映射
    - 支持 `序号/编码/名称/数量/位号/封装/规格` 等中文列名

### 其他

17. **40MHz晶振匹配不严谨** ← 修复频率匹配精度
    - 问题：频率数字只包含匹配，24MHz也匹配了40MHz
    - 修复：`spec_freq_match.group(1) == target_freq` 确保频率完全一致

18. **0.1R/F/0.5W电阻值解析** ← 修复R作为欧姆单位
    - Value中 `R` 等效为欧姆Ω
    - 功率 `/0.5W` 对应 `1/2W`，`/0.0625W` 对应 `1/16W`

### v2.1.0 新增

19. **电感匹配** ← 新增 `find_inductor` 函数
    - 支持 `IND` 封装识别，解析 `uH`/`nH` 感值
    - 额定电流解析（`/4A` 格式）
    - 按感值精确匹配+封装+电流评分排名

20. **NP/NC/NM 空贴检测** ← 新增备注列
    - 值整体等于 `NC`/`NP`/`NM` → 跳过不处理
    - 值含 `/NC`、`/NP`、`_NC`、`_NP` 等 → 保留行标记 `⚠️请确认是否空贴`

21. **元件分类优先级新增电感** ← 优化分类顺序
    - 电感（IND封装/UH值）插入磁珠之后、电阻之前
    - IC→晶振→磁珠→电感→电阻→电容→通用