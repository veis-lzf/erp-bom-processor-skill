---
name: erp-bom-processor
description: "处理PCB BOM文件进行元件库匹配、值/封装搜索和格式生成。当用户需要将原理图BOM转换为格式化的ERP BOM时、或做BOM差异比对时调用。Trigger: BOM, ERP BOM, PCB BOM, BOM处理, 原理图BOM, 物料匹配, 元件库匹配, BOM比对, BOM差异, bom processor, bill of materials"
---

# ERP BOM处理器

## Agent 执行流程

当用户请求处理 BOM 或比对 BOM 时，按以下步骤执行：

### 场景 A: 单个 BOM 处理

| 步骤 | 操作 | 输入 | 输出 |
|------|------|------|------|
| A1 | 确认工作区目录存在（`02_BOMfromSystem/`、`03_order/`、`04_output/`） | 用户指定的工作目录 | 目录就绪状态 |
| A2 | 识别最新元件库文件（选 `02_BOMfromSystem/` 下日期最新的 `.xlsx`） | 目录文件列表 | `LIB_FILE` 路径 |
| A3 | 识别待处理 BOM 文件（`03_order/` 下 `.xlsx` 文件） | 目录文件列表 | `BOM_FILES` 列表 |
| A4 | 🛑 STOP — 向用户确认：库文件版本 + BOM 文件列表 + 输出目录 | 已识别的文件路径 | 用户确认（是/否/修改） |
| A5 | 运行 BOM 处理脚本（当前目录下已有 `bom_processor.py` 直接执行；否则从源码包复制：`copy erp-bom-processor\scripts\bom_processor.py .`） | 确认后的文件路径配置 | `_processed.xlsx` 输出文件 |
| A6 | 🔴 CHECKPOINT — 验证输出：检查匹配统计、未匹配行、空贴标记 | 输出文件 | 验证通过 / 需修正 |

### 场景 B: 多 BOM 差异比对

| 步骤 | 操作 | 输入 | 输出 |
|------|------|------|------|
| B1 | 确认 `04_output/` 下存在 ≥2 个 `_processed.xlsx` 文件 | 输出目录 | 可比对文件列表 |
| B2 | 运行 BOM 比对脚本（需先从源码包复制 `bom_diff.py`：`copy erp-bom-processor\scripts\bom_diff.py .`，然后 `python bom_diff.py <文件1> <文件2> ...`） | 文件路径列表 | `BOM差异清单.xlsx` |
| B3 | 🔴 CHECKPOINT — 验证差异：确认独有物料、数量变化的合理性 | 差异清单 | 验证通过 / 需排查 |

---

## 概述

处理从原理图工具导出的PCB BOM，与元件库（系统电子料库）进行匹配，并生成格式化的ERP BOM文件。

**核心功能**：
- 有料号的通过料号从电子料库索引最新物料描述
- 无料号的通过物料属性值(Value)+器件封装(Footprint)匹配，提取料号和描述
- 电阻/电容/磁珠/电感/晶振等元件类型自动识别与匹配
- 电阻优先匹配1%精度，电容无满足耐压时匹配更高耐压
- 电感按感值+额定电流+封装匹配
- 相同料号自动合并，位号用逗号拼接
- 中英文BOM列名格式兼容
- NP/NC/NM 空贴物料检测与标记（输出备注列）
- 多BOM差异比对，生成差异清单List

## 资源文件

| 文件 | 位置 | 用途 |
|------|------|------|
| `SKILL.md` | 当前目录 | 本 Skill 定义文件 |
| `bom_processor.py` | 当前目录（与 SKILL.md 同级） | 核心 BOM 处理脚本，可直接运行 |

以下文件位于源码包 `d:\TRAE\Home\erp-bom-processor\scripts\`，需要时复制到工作区：

| 文件 | 源路径 | 用途 |
|------|--------|------|
| `bom_diff.py` | `erp-bom-processor/scripts/bom_diff.py` | BOM 差异比对脚本 |
| `test_runner.py` | `erp-bom-processor/scripts/test_runner.py` | 自动化测试运行器 |
| `install.js` | `erp-bom-processor/install.js` | npm 安装脚本 |
| `bin/cli.js` | `erp-bom-processor/bin/cli.js` | CLI 入口（`npx erp-bom`） |
| `package.json` | `erp-bom-processor/package.json` | 包配置（v2.2.0） |

### 工作区目录结构

处理 BOM 时在工作目录下创建以下结构：

```
erp-bom-workspace/
├── 01_Template/          # BOM模板参考文件（可选，用于格式对照）
├── 02_BOMfromSystem/     # 元件库文件 → 放入最新日期的 .xlsx
├── 03_order/             # 待处理的BOM文件 → 放入原理图导出的 .xlsx
├── 04_output/            # 输出目录 → 生成 _processed.xlsx 和 BOM差异清单.xlsx
├── bom_processor.py      # 从脚本目录复制至此（或直接运行 scripts/bom_processor.py）
└── bom_diff.py           # 从脚本目录复制至此（或直接运行 scripts/bom_diff.py）
```

> 脚本文件位于源码包 `scripts/` 目录下。可复制到工作区运行，或通过 `python <skill安装路径>/scripts/bom_processor.py` 直接调用。

## BOM列映射

支持两种列名格式：

### 英文格式

| 输入列           | 内部列名       | 描述       |
| ------------- | ----------- | -------- |
| Item          | Item        | 序号       |
| Part NO.      | Part NO.    | 元件料号（可选） |
| Part Name     | Part Name   | 元件描述（输入） |
| Quantity      | Quantity    | 元件使用数量   |
| Reference     | Reference   | 元件位号     |
| PCB Footprint | PCB Footprint | 器件封装     |
| Value         | Value       | 元件属性值    |

### 中文格式

| 输入列 | 内部列名 | 描述       |
| --- | --- | -------- |
| 序号  | Item | 序号       |
| 编码  | Part NO. | 元件料号（可选） |
| 名称  | Part Name | 元件描述（输入） |
| 数量  | Quantity | 元件使用数量   |
| 位号  | Reference | 元件位号     |
| 封装  | PCB Footprint | 器件封装     |
| 规格  | Value | 元件属性值    |

## 处理规则

### 总体流程图

```
BOM输入
  │
  ├─ 跳过条件：位号TP开头 或 值包含NC
  │
  ├─ 有料号 ──→ 通过料号搜索库 ──→ 验证电阻值 ──→ 找到则用
  │                                                    │
  └─ 无料号或料号无效 ──→ 值+封装匹配 ──→ 类型分类 ──→ 类型特定匹配
                                                              │
                                                    ┌─── 找到 → 提取料号+描述
                                                    └─── 未找到 → 标记"未找到匹配"
```

### 1. 料号匹配

- 如果提供了料号，首先通过料号搜索
- 如果找到，验证电阻值是否匹配（针对电阻）
- 如果未找到或无效，使用值+封装匹配

### 2. 值+封装匹配

#### 元件类型分类（严格优先级顺序）

按以下**优先级顺序**判断元件类型，一旦匹配即进入对应类型的匹配函数：

1. **IC/芯片** — 值以以下开头：RTL、STM、ATMEGA、ESP、GD32、NXP、TI、MICROCHIP、CA-
2. **晶振** — 封装满足以下任一条件：
   - 以 `X4-` 开头（如 `X4-3225`）
   - 包含 `_X4_`
   - 以 `_X4` 结尾
   - 包含 `XTAL` 或 `OSC`
3. **磁珠(FB)** — 位号以 `FB` 开头，或封装中包含 `FB`
4. **电感** — 封装以 `IND` 开头，或值中包含 `UH`/`NH`
5. **电阻** — 封装以 `R` 开头，或包含 `RES`，或值中包含 `OHM`，或同时包含 `K` 和 `F`，或包含 `R/`，或4位数字后跟 `R`，或包含 `M/F`，或封装结尾是 `R` 且前面是数字
6. **电容** — 封装以 `C` 开头，或包含 `CAP`，或值中包含 `F`（排除以下情况：K+F、R/、M/、RTL/STM开头、FB位号、含`_`/`-`且非电压结尾）
7. **通用元件** — 其他所有元件

> **注意**：`MHZ` 值作为磁珠的补充判断（在电容判断之后），仅当以上条件都不满足且值包含 `MHZ` 时才判断为磁珠。例如 `330R/100MHZ` 且位号 `FB` 开头会优先走磁珠判断。

#### 电阻匹配规则

**值解析流程** (`parse_resistor_value`)：
1. 精度识别：`1%`/`F` → 1%，`5%`/`J` → 5%，默认5%
2. 清理值：去除 `1%`/`5%`/`F`/`J`/`/0.5W`/`/100MHZ` 等后缀
3. 单位匹配（优先级从高到低）：
   - `MR` → 兆欧（×1000000）
   - `mR` → 毫欧（÷1000），**优先于 `m`** 避免误判
   - `m` → 毫（÷1000）
   - `M` → 兆（×1000000）
   - `K` → 千（×1000）
   - `R/`、`R`+结束/空格/下划线 → 欧姆
   - `OHM` → 欧姆
   - 纯数字带Ω → 欧姆

**分隔符支持**：`_`、`-`、`/` 作为分隔符，例如 `60.4R_5%`、`10mR/F/0.5W`、`3M/F`

**库中值提取** (`extract_resistance_value`)：
- 从规格型号中提取电阻值，如 `100mΩ,±1%,1/2W,1210,厚声,...` → 100mΩ → 0.1Ω
- 支持 KΩ、MΩ、Ω、mΩ 单位

**功率解析** (`parse_power`)：
- 从 `/(\d+\.?\d*)W` 中提取功率值
- 转换：0.0625W→1/16W，0.125W→1/8W，0.25W→1/4W，0.5W→1/2W，1.0W→1W

**封装格式兼容**：
- `R0805` → 提取 `0805`
- `0805R` → 提取 `0805`
- 在库规格中匹配 `0805` 即为匹配

**严格匹配规则**：
- 如果BOM指定了封装（如 `R0805`），则库物料规格中必须包含 `0805`
- 库中无对应封装物料时 → **返回None**（不会匹配其他错误封装的物料）
- 优先级：正确封装(必须) > 精度优先(1%)

#### 电容匹配规则

**值解析流程** (`parse_capacitor_value`)：
- 正则：`^([\d.]+)\s*([PFNU]F?)\s*(?:[/_-]?(\d+V))?$`
- 支持分隔符：`/`、`_`、`-`
- 示例：`100nF/50V`、`100pF_50V`、`10uF-50V`、`330pF`、`47uF 16V`
- 单位转换：PF/P→×1，NF/N→×1000，UF/U/μF→×1000000
- 电压提取：`50V` → 50

**库中值提取** (`extract_capacitance_value`)：
- 从规格中提取容值，如 `47uF,±10%,16V,X5R,1210,...` → 47uF
- 单位转换与解析一致

**电压提取** (`extract_voltage_from_spec`)：
- 从规格中提取电压值，如 `16V` → 16
- 正则：`(?:^|，|,)(\d+)V(?:,|$|\s)`

**封装格式兼容**：
- `C0805` → 提取 `0805`
- `0805C` → 提取 `0805`
- `1210C` → 提取 `1210`（通过 `C?(\d{4})C?` 正则）

**严格匹配规则**：
- 如果BOM指定了封装（如 `C0402`），则库物料规格中必须包含对应封装编号
- 库中无对应封装物料时 → **返回None**
- 优先级：正确封装(必须) > 最近电压(0)，电压为None时视为 `float('inf')`

#### 晶振匹配规则

**频率匹配**：
- 从值中提取频率数字：`40Mhz` → `40`
- 从库规格中提取频率数字：`40MHz` → `40`
- **必须完全匹配**（字符串相等），避免24MHz匹配到40MHz

**封装匹配**：
- 从封装中提取4位数字编号：`X4-3225` → `3225`
- 库规格中包含该编号即为封装匹配

**多结果支持**：
- `find_crystal` 支持 `return_all=True` 参数，返回所有匹配候选
- 优先级：正确封装(2) > 仅频率匹配(0)

#### 磁珠匹配规则

**识别条件**：
1. 位号以 `FB` 开头（如 `FB1`、`FB2`）
2. 封装中包含 `FB`
3. 值中包含 `MHZ` 作为补充判断（如 `330R/100MHZ`）

**严格匹配规则**：
- 从封装中提取4位数字编号
- 库物料名称需包含 `磁珠`/`BEAD`/`FERRITE`
- 库物料规格中必须包含对应封装编号
- 无匹配时 → **返回None**

#### 电感匹配规则

**识别条件**：
1. 封装以 `IND` 开头（如 `IND_201610`、`IND_252012`）
2. 值中包含 `UH`（微亨）或 `NH`（纳亨）

**值解析流程** (`find_inductor`)：
- 支持格式：`4.7uH`、`2.2uH`、`47nH`、`1uH`、`4.7uH/4A` 等
- 单位统一转换为 nH 进行比较：
  - `UH`/`uH` → ×1000
  - `NH`/`nH` → ×1
- 解析额定电流：`/4A`、`,4A` → I=4A

**匹配规则**：
- 库物料名称需包含 `电感`/`绕组`/`INDUCTOR`/`IND`
- 电感值必须精确匹配（≤0.1% 误差）
- 封装匹配加分（+5），额定电流匹配加分（+3）
- 按总分排序取最高分，无匹配时 → 回退到 `find_generic`

#### 通用元件匹配 (`find_generic`)

**特殊字符处理**：
- 去除 `_` 和 `-` 后进行模糊匹配
- 解析 `HFD4_5-SR` → `HFD45SR` 进行匹配

**封装关键词提取**：
- `BAT_XT30PWPW2_PB` → 提取 `XT30PW`（优先匹配卧式）
- `BAT_XT30PBPB2_PB` → 提取 `XT30PB`
- 含 `XT30` 或 `XT60` 的封装也会匹配

**评分机制**（按总分排序，取最高分）：
| 条件 | 分值 |
| --- | --- |
| 值在规格中（含模糊匹配） | 2 |
| 值在名称中 | 1 |
| 封装在规格或名称中 | 1 |
| 封装关键词在规格中 | 3 |

#### IC匹配规则 (`find_ic`)

- 值或规格互相包含即视为候选
- 优先返回规格中精确包含值的物料
- 支持型号如 `RTL8762CMF`、`STM32F103`、`CA-IF1051VS` 等

### 3. 特殊跳过情况

- **位号以"TP"开头**（测试点，不参与BOM处理）
- **值整体等于 `NC`/`NP`/`NM`**（空贴/不焊接，跳过）
- **值包含 `/NC`、`/NP`、`_NC`、`_NP`、`NC_`、`NP_`**（疑似空贴，保留行并在备注列标记）

### 4. 备注列

输出BOM中包含 `备注` 列，用于标记：
- `⚠️请确认是否空贴` — 值包含 `/NC`、`/NP`、`_NC`、`_NP` 等疑似空贴标记，需要人工确认

### 5. 相同料号合并

- 合并相同料号的行（无料号的不合并）
- 数量求和
- 位号用逗号连接（如 `R1,R4,R7`）

## 失败模式与异常处理

以下按处理流程顺序列出所有已知失败场景及其恢复路径。每条包含**触发条件 → 一线修复 → 仍失败兜底**三段式。

### 环境与文件

| # | 触发条件 | 一线修复 | 仍失败兜底 |
|---|---------|---------|-----------|
| E1 | 元件库文件 `02_BOMfromSystem/` 为空或不存在 | 提示用户放入最新元件库文件并指定路径 | 若无法获取库文件 → 终止处理，输出错误日志 `⚠无元件库，无法匹配` |
| E2 | 元件库文件存在但格式非 `.xlsx` | 提示用户确认文件格式，尝试 `pd.ExcelFile()` 加载 | 加载失败 → 跳过该库文件，尝试下一个日期最新的文件 |
| E3 | BOM 文件 `03_order/` 内无 `.xlsx` 文件 | 提示用户放入待处理的 BOM 文件 | 无文件 → 终止，输出 `⚠未找到BOM文件` |
| E4 | BOM 文件列名不匹配（既非英文也非中文列名） | 打印实际列名，尝试相似度匹配 | 无法映射 → 跳过该文件，在输出目录生成 `_error.log` 记录失败原因 |

### 料号匹配

| # | 触发条件 | 一线修复 | 仍失败兜底 |
|---|---------|---------|-----------|
| M1 | BOM 中有料号但库中搜索不到 | 尝试模糊匹配：去除空格/特殊字符后重搜 | 仍然找不到 → 回退到**值+封装匹配**（`find_component_by_value_footprint`） |
| M2 | 料号匹配成功但电阻值验证不一致 | 标记该行，输出警告 `⚠料号匹配但阻值不匹配` | 不采用该匹配结果 → 回退到**值+封装匹配** |
| M3 | 料号匹配结果有多条（库数据重复） | 选第一条匹配结果 | 不适用（库中料号应唯一） |

### 类型分类

| # | 触发条件 | 一线修复 | 仍失败兜底 |
|---|---------|---------|-----------|
| T1 | 值/封装特征不匹配任何已知类型 | 按分类优先级顺序逐级判断，不跳过任何一级 | 最终落入**通用元件**（`find_generic`），通过模糊匹配+评分尝试匹配 |

### 各类型匹配

| # | 触发条件 | 一线修复 | 仍失败兜底 |
|---|---------|---------|-----------|
| F1 | 电阻匹配：封装严格匹配无结果 | 确认封装格式兼容（`R0805`/`0805R`/`0805`）后重试 | 返回 **None**，不匹配其他封装的物料 → 输出标记 `未找到匹配` |
| F2 | 电容匹配：封装严格匹配无结果 | 确认封装格式兼容（`C0805`/`0805C`/`1210C`）后重试 | 返回 **None** → 输出标记 `未找到匹配` |
| F3 | 电容匹配：电压为None时排序报错 `TypeError` | 将 None 电压值转换为 `float('inf')` 再排序 | 按容值最接近原则降级匹配（忽略电压） |
| F4 | 晶振匹配：频率不精确匹配到错误晶振 | 频率必须精确字符串相等（`spec_freq == target_freq`），禁止包含匹配 | 返回 None 或 `return_all=True` 列出所有候选供人工选择 |
| F5 | 磁珠匹配：封装严格匹配无结果 | 确认封装格式后重试 | 返回 **None** → 输出标记 `未找到匹配` |
| F6 | 电感匹配：无精确匹配结果 | 放宽电流约束重新搜索 | 回退到 **`find_generic`** |
| F7 | 通用匹配：模糊匹配评分后无候选 | 去除 `_` 和 `-` 特殊字符后重试，提取封装关键词（如 `XT30PW`）辅助匹配 | 返回 None → 输出标记 `未找到匹配` |

### 数据后处理

| # | 触发条件 | 一线修复 | 仍失败兜底 |
|---|---------|---------|-----------|
| P1 | 相同料号合并时遇到无料号行 | 跳过该行，不参与合并 | 无料号行保留原始数据不变 |
| P2 | BOM 比对时非 `_processed.xlsx` 文件混入 | 通过文件名过滤（必须含 `_processed`） | 非 processed 文件跳过，并在终端打印警告 |

### 值解析异常

| # | 触发条件 | 一线修复 | 仍失败兜底 |
|---|---------|---------|-----------|
| V1 | 电阻值解析失败（未知格式） | 逐个尝试已知正则模式（MR/mR/M/K/R/OHM） | 全部失败 → 返回 None，输出 `⚠无法解析电阻值: {value_str}` |
| V2 | 电容值解析失败（未知格式） | 逐个尝试分隔符 `/_`/`-` 并匹配正则 `([\d.]+)\s*([PFNU]F?)\s*(?:[/_-]?(\d+V))?` | 全部失败 → 返回 None，输出 `⚠无法解析电容值: {value_str}` |
| V3 | 功率解析失败（无 `/xW` 模式） | 返回 None，不影响后续匹配 | 匹配时忽略功率要求 |

> 以上失败模式直接来自「关键修复记录」中的 19 条实际踩坑经验，以及 `bom_processor.py` 中显式的 `return None` 逻辑路径。

## 核心函数说明

### 值解析函数

| 函数 | 输入 | 输出 | 说明 |
| --- | --- | --- | --- |
| `parse_resistor_value(value_str)` | 电阻值字符串 | `(value_ohms, precision)` | 解析电阻值和精度，支持多种格式 |
| `parse_capacitor_value(value_str)` | 电容值字符串 | `(value_pf, voltage)` | 解析电容值和电压，支持多种分隔符 |
| `parse_power(value_str)` | 值字符串 | 功率分数或None | 解析功率值，如 `/0.5W` → `1/2W` |

### 库值提取函数

| 函数 | 说明 |
| --- | --- |
| `extract_resistance_value(spec)` | 从库规格中提取电阻值（Ω） |
| `extract_capacitance_value(spec)` | 从库规格中提取电容值（pF） |
| `extract_voltage_from_spec(spec)` | 从库规格中提取电压值（V） |
| `validate_resistor_match(component, value_str)` | 验证料号匹配的电阻值是否一致 |

### 匹配函数

| 函数 | 说明 |
| --- | --- |
| `find_component_by_part_no(lib, part_no)` | 通过料号精确查找 |
| `find_component_by_value_footprint(lib, value, footprint, reference)` | 值+封装匹配，自动类型分类 |
| `find_ic(lib, value_str, footprint_str)` | IC/芯片匹配 |
| `find_resistor(lib, value_str, footprint_str)` | 电阻匹配（含封装严格匹配） |
| `find_capacitor(lib, value_str, footprint_str)` | 电容匹配（含封装严格匹配） |
| `find_bead(lib, value_str, footprint_str)` | 磁珠匹配（含封装严格匹配） |
| `find_inductor(lib, value_str, footprint_str)` | 电感匹配（感值+电流+封装，无匹配回退通用） |
| `find_crystal(lib, value_str, footprint_str, return_all=False)` | 晶振匹配（频率精确匹配） |
| `find_generic(lib, value_str, footprint_str)` | 通用元件匹配（模糊匹配+评分） |

### 处理函数

| 函数 | 说明 |
| --- | --- |
| `merge_by_part_no(df)` | 合并相同料号的数据行 |
| `process_bom(bom_file, lib_file, output_dir, merge_same_part_no=True)` | 主处理流程 |

### 比对函数 (`bom_diff.py`)

| 函数 | 说明 |
| --- | --- |
| `load_bom_files(file_paths)` | 加载多个processed BOM文件 |
| `build_diff_data(bom_data)` | 构建以Part NO.为索引的比对数据结构 |
| `generate_diff_report(...)` | 生成差异报告（汇总/独有/共有差异/未匹配） |
| `write_diff_excel(...)` | 输出格式化的差异清单Excel |
| `bom_diff(file_paths, output_path)` | 比对主入口函数 |

## 调试辅助脚本

源码包 `scripts/` 目录中包含以下调试脚本，用于验证特定功能：

| 脚本 | 用途 |
|------|------|
| `check_all_fp.py` | 检查所有元件的封装匹配情况 |
| `verify_fixes.py` / `verify_latest_fixes.py` | 验证修复是否生效 |
| `debug_bead.py` / `debug_bead2.py` | 磁珠匹配调试 |
| `debug_cap_format.py` / `debug_cap_match.py` | 电容格式/匹配调试 |
| `debug_resistor.py` / `debug_resistor_match.py` | 电阻匹配调试 |
| `debug_hfd4.py` / `test_hfd4.py` | HFD4继电器匹配调试 |
| `debug_process.py` | 完整处理流程调试 |
| `verify_output.py` | 输出结果验证 |

> 调试脚本位于源码包 `scripts/` 目录。运行方式：`cd scripts && python debug_bead.py`

## 反例与黑名单

以下操作**禁止**执行，违反将导致数据损坏或匹配错误：

### 数据安全

| # | 禁止行为 | 原因 | 替代做法 |
|---|---------|------|---------|
| 1 | 直接修改 `02_BOMfromSystem/` 下的元件库源文件 | 破坏元件库数据完整性，影响所有后续 BOM 处理 | 如有数据错误，在源系统修正后重新导出 |
| 2 | 原地覆盖 `03_order/` 下的原始 BOM 文件 | 丢失原始数据，无法追溯和复验 | 输出写至 `04_output/`，保留原始文件不变 |
| 3 | 手动编辑 `04_output/` 下已处理完成的 BOM 文件 | 破坏处理结果的可复现性，下次重跑会被覆盖 | 修改匹配规则或库数据后重新运行脚本 |

### 匹配逻辑

| # | 禁止行为 | 原因 | 替代做法 |
|---|---------|------|---------|
| 4 | 跳过封装验证去匹配其他封装的物料 | 导致元件封装错误，PCB 无法焊接 | 封装严格匹配无结果时返回 None，标记"未找到匹配" |
| 5 | 在分类判断中绕过优先级顺序直接猜类型 | 会重蹈 HFD4 继电器误判为电容、CA-IF1051VS 误判为晶振等已知错误 | 必须按 IC→晶振→磁珠→电感→电阻→电容→通用 的顺序逐级判断 |
| 6 | 用模糊匹配替代精确匹配（电阻值、电容值、晶振频率） | 导致错误物料匹配，如 24MHz 晶振匹配到 40MHz | 频率/阻值/容值必须精确相等，不能用包含匹配 |

### 流程操作

| # | 禁止行为 | 原因 | 替代做法 |
|---|---------|------|---------|
| 7 | 未加载最新元件库就运行 BOM 处理 | 旧库可能缺少新物料或含已淘汰料号 | 始终使用 `02_BOMfromSystem/` 中日期最新的文件 |
| 8 | 跳过空贴检测（NP/NC/NM）直接合并输出 | 空贴物料混入 BOM 导致采购和焊接错误 | 运行 `process_bom()` 时确保空贴检测生效，检查备注列 |
| 9 | 在无料号的情况下合并行 | 不同物料可能被错误合并为同一行 | `merge_by_part_no()` 只合并有料号的行 |

### 输出验证

| # | 禁止行为 | 原因 | 替代做法 |
|---|---------|------|---------|
| 10 | 跳过 `verify_output.py` 验证直接交付 BOM 结果 | 未经验证的 BOM 输出可能遗漏匹配失败行或封装错误 | 处理完成后运行验证脚本，确认无未匹配项或已知可接受的未匹配 |

> 以上规则来自本项目实际踩过的坑（19 条修复记录），每一条都是真实出现过的错误。

## 配置参考

如需直接编辑脚本参数（非通过 Agent 执行流程），修改 `bom_processor.py` 底部的 `__main__` 部分：

```python
BOM_FILES = [
    '03_order/你的BOM文件.xlsx',
]
LIB_FILE = '02_BOMfromSystem/系统电子料库_最新日期.xlsx'
OUTPUT_DIR = '04_output'
```

`bom_processor.py` 默认支持批量处理多个 BOM 文件，在 `BOM_FILES` 列表中添加路径即可。

## BOM 比对输出结构

`bom_diff.py` 生成 `BOM差异清单.xlsx`（位于 `04_output/`），包含 4 个 Sheet：

| Sheet名称 | 说明 |
| --- | --- |
| `汇总统计` | 各BOM的总物料数、已匹配/未匹配数量、匹配率 |
| `独有物料` | 仅在某一BOM中出现（其他BOM中不存在的料号），按BOM名称彩色标记 |
| `共有物料差异` | 多个BOM共有的物料，但数量或位号存在差异，黄色高亮标记 |
| `未匹配物料` | 所有BOM中未携带有效料号的物料，红色高亮标记 |

比对逻辑：以 `Part NO.` 为主键比对，无料号物料归入「未匹配物料」。

## 版本

| 版本 | 更新内容 |
| --- | --- |
| **v1.0** | 完整功能实现，7类元件匹配，3份BOM自测通过 |
| **v1.1** | 新增 `bom_diff.py` 多BOM差异比对 |
| **v2.1.0** | 新增电感匹配、NP/NC/NM空贴检测与备注列、CLI参数支持 |
| **v2.2.0** | 当前版本 |

## 修复历史

项目共经历 19 条修复（详见上方「失败模式与异常处理」各表）。高影响修复包括：分类优先级顺序调整（#1-#3）、正则解析精度增强（#4-#8,#17）、封装严格匹配机制（#9-#12,#14）、料号合并（#13）、空贴检测（#19-#20）、电感匹配（#18）。